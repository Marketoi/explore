*" Idea worth not much, execution is king. "*

*" with AI, execution cost nothing "*
... not true.

But there may be some truth in between, where -> execution cost can be reduced by orders of magnitude, if : 

AI just has to code small bytes ; part of a complex/powerful solution.
A bit similar to microservices principle, some kind of fractal microservice

Approach : 
A combination where each part has a very small scope of concern, whether it's micro-service or micro-frontends.

AI would code those very little scope with no impact on the others, cloisonnement.

TDD : by design, test first
and make sure no drift in AI coding.

Smaller code base : for example, rely on cloud services such as AWS/cognito.

And reuse/improve an optimized [[00006 - App starter]]


As a MVP, some creative guys sometimes just do a landing page (or 100 lp...) and test which one would bring best response from potential adopters. Famous MVP of _dropbox_ ...
Here it offers a similar approach where MVP goes a bit further than LandPage but : included a working prototype , at very low cost.